import { RatingToRecomendPipe } from './rating-to-recomend.pipe';

describe('RatingToRecomendPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingToRecomendPipe();
    expect(pipe).toBeTruthy();
  });
});
